<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.reyad.psycheadminpanel">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PsycheAdminPanel">
        <activity android:name=".main.teacher.ui.main.TeacherInformation"></activity>
        <activity
            android:name=".main.teacher.TeacherDataActivity"
            android:label="@string/title_activity_teacher_data"
            android:theme="@style/Theme.PsycheAdminPanel.NoActionBar" />
        <activity android:name=".main.teacher.TeacherUpload" />
        <activity android:name=".main.student.StudentUpdate" />
        <activity android:name=".main.student.StudentView" />
        <activity android:name=".main.student.StudentActivity" />
        <activity android:name=".main.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".main.StudyActivity" />
        <activity android:name=".main.MainActivity" />
    </application>

</manifest>